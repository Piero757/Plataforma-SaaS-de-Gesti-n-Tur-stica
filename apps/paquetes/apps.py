from django.apps import AppConfig

class PaquetesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.paquetes'
    verbose_name = 'Gestión de Paquetes Turísticos'
