# CONFIGURACIÓN PARA PRUEBAS LOCALES
# Copia este contenido a tu archivo .env

# OPCIÓN 1: SQLite (Sin instalar PostgreSQL - RECOMENDADO PARA PRUEBAS RÁPIDAS)
DB_NAME=sqlite:///db.sqlite3

# OPCIÓN 2: PostgreSQL (Si ya tienes PostgreSQL instalado)
# DB_NAME=turismo_db
# DB_USER=postgres
# DB_PASSWORD=tu_contraseña_postgres
# DB_HOST=localhost
# DB_PORT=5432

# Configuración Django
SECRET_KEY=django-insecure-key-demo-local-pruebas-super-segura
DEBUG=True

# Configuración Redis (opcional, no necesario para pruebas básicas)
REDIS_URL=redis://localhost:6379/0

# INSTRUCCIONES:
# 1. Crea un archivo llamado .env en la raíz del proyecto
# 2. Copia el contenido de arriba
# 3. Si usas SQLite, solo usa la línea DB_NAME=sqlite:///db.sqlite3
# 4. Si usas PostgreSQL, comenta la línea de SQLite y descomenta las de PostgreSQL
